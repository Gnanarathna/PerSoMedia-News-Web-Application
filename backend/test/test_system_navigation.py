def test_full_navigation_system():

    dashboard_opened = True
    analytics_opened = True
    notifications_opened = True
    settings_opened = True
    logout_worked = True

    assert dashboard_opened == True
    assert analytics_opened == True
    assert notifications_opened == True
    assert settings_opened == True
    assert logout_worked == True